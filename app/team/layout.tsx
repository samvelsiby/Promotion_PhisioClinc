import { pageMetadata } from '@/lib/metadata'

export const metadata = pageMetadata("Meet Our Team | Physiotherapists in Winnipeg | Pro Motion", "Meet the physiotherapists and support team at Pro Motion Physiotherapy in St. Vital, Winnipeg. Explore their qualifications and backgrounds.", "/team")

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children
}
