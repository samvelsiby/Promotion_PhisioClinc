import { pageMetadata } from '@/lib/metadata'

export const metadata = pageMetadata("Direct Billing & Physiotherapy Insurance | Pro Motion Winnipeg", "See insurance providers we direct bill at Pro Motion Physiotherapy in Winnipeg. Contact our St. Vital clinic with questions about your coverage.", "/insurance")

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children
}
