import { pageMetadata } from '@/lib/metadata'

export const metadata = pageMetadata("About Pro Motion Physiotherapy | St. Vital, Winnipeg", "Visit Pro Motion Physiotherapy at Unit 4, 567 St. Anne’s Road, Winnipeg. Learn about our clinic, hours, direct billing and appointment options.", "/about")

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children
}
