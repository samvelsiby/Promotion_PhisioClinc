import { pageMetadata } from '@/lib/metadata'

export const metadata = pageMetadata("About Pro Motion Physiotherapy | St. Vital, Winnipeg", "Visit Pro Motion in Meadowood, St. Vital, at Unit 4, 567 St. Anne’s Road, Winnipeg. Find clinic hours, direct billing and appointment information.", "/about")

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children
}
