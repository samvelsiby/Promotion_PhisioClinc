import { pageMetadata } from '@/lib/metadata'

export const metadata = pageMetadata("How We Treat | Physiotherapy in Winnipeg | Pro Motion", "Explore manual therapy, exercise and rehabilitation at Pro Motion Physiotherapy in St. Vital, Winnipeg. Care for pain, sport, MPI and WCB injuries.", "/how-we-treat")

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children
}
