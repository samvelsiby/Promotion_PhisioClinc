import { pageMetadata } from '@/lib/metadata'

export const metadata = pageMetadata("All Treatment Conditions | Pro Motion Physiotherapy Winnipeg", "Browse all conditions treated at Pro Motion Physiotherapy in St. Vital, Winnipeg, including back pain, sports injuries and rehabilitation needs.", "/what-we-treat/all")

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children
}
