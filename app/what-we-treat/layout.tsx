import { pageMetadata } from '@/lib/metadata'

export const metadata = pageMetadata("Conditions We Treat | Physiotherapy in Winnipeg | Pro Motion", "Explore physiotherapy for back pain, sports injuries, joint pain and more at Pro Motion Physiotherapy in St. Vital, Winnipeg. Book your assessment.", "/what-we-treat")

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children
}
